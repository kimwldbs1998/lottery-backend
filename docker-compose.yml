services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cenlottery
      POSTGRES_USER: cenlottery
      POSTGRES_PASSWORD: cenlottery
    ports:
      - "5432:5432"
    volumes:
      - cenlottery-pgdata:/var/lib/postgresql/data

volumes:
  cenlottery-pgdata:
